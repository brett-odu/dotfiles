---
- name: Install and configure Zsh with plugins
  ansible.builtin.import_tasks: install_zsh.yml
  tags: [zsh, shell]

- name: Set Zsh as default shell
  ansible.builtin.import_tasks: set_default_shell.yml
  tags: [zsh, shell, default]
