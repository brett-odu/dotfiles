---
- name: Install Docker and Setup Docker Group
  ansible.builtin.include_tasks: install_container_engine.yml
  tags:
    - container
    - docker
    - podman
    - install
    - group
    - verify

- name: Install Helm
  ansible.builtin.include_tasks: install_helm.yml
  tags:
    - helm
    - install
    - verify
